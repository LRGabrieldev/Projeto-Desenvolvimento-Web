Objetivo: Aplicação de gerenciamento financeiro
Login --> Cadastro --> App (Não trabalhar no app por hora, apenas página em branco)
API: Web Storage (Exclusiva ao lado do cliente)
Json

Json lê os inputs de login e senha e envia para o Web Storage.

Propriedade location do JS para redirecionar usuario.

Precisa das 3 páginas para funcionar (Cadastro, Login, APP)

Não pode permitir que o usuario se autentique pela página principal (Usar a url e ir direto para o APP),
é necessario fazer uma verificação em js para ver se o usuario está logado. 